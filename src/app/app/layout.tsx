export const metadata = {
  title: "SafeBox — Stock en direct",
  description: "Consultez le stock disponible dans votre distributeur SafeBox",
};

export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
